source 'https://rubygems.org'

gem 'rails', '3.2.3'
gem 'nokogiri'
gem 'heroku'

group :assets do
	gem 'uglifier', '>= 1.0.3'
	gem 'sass-rails'
	gem 'railties'
end

group :development, :test do
        gem 'sqlite3'
end

group :production do
        gem 'pg'
        gem 'thin'
end
